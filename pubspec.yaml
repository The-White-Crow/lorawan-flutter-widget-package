name: flutter_widget_package
description: "A new Flutter package project."
version: 0.0.1
publish_to: 'none' 

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

  
dependencies:
  flutter:
    sdk: flutter
  
  flutter_core_package:
    git: https://github.com/The-White-Crow/lorawan-flutter-core-package

  # UI / Widget
  flutter_screenutil: ^5.9.3
  toastification: ^3.2.0
  marquee: ^2.3.0

  # State management
  flutter_riverpod: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  very_good_analysis: ^10.3.0

flutter:

